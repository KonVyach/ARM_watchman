#include "facecontrol/core/Person.h"

// Currently nothing to define out-of-line — kept as a compilation unit so the
// CMakeLists.txt entry has a target file. Functions are inline in the header.

namespace fc {}
